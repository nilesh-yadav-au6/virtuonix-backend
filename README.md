#steps to locally install and run

- git clone git@github.com:nilesh-yadav-au6/virtuonix-backend.git
- Open the project in vs code
- Create a .env file and copy backend .env settings as shared in the mail
- npm install
- npm start
